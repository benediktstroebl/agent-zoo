[project]
name = "agent-zoo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "attrs>=24.3.0",
    "docker>=7.1.0",
    "python-dotenv>=1.0.1",
    "pyyaml>=6.0.2",
]
